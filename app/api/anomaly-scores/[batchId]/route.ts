export async function GET(_r:Request,c:{params:Promise<{batchId:string}>}){const {batchId}=await c.params;return Response.json({batchId,score:null,level:"unavailable",reasons:["Persistent anomaly storage has not been configured on Vercel."],verifiedForkAttempts:0},{status:503});}
